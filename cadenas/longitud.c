#include <stdio.h>
#include <string.h>

int main() {
    char cadena[] = "Ejemplo de longitud";
    size_t longitud = strlen(cadena);
    printf("Longitud de la cadena: %zu\n", longitud);

    return 0;
}
